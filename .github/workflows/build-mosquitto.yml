name: Build Mosquitto Image

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'docker/mosquitto/**'
      - '.github/workflows/build-mosquitto.yml'

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build and push mosquitto image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@b5ca514318bd6ebac0fb2aedd5d36ec1b5c232a2 # v3.10.0

      - name: Login to GitHub Container Registry
        uses: docker/login-action@74a5d142397b4f367a81961eba4e8cd7edddf772 # v3.4.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          # GITHUB_TOKEN 推送 ghcr 报 permission_denied(write_package),与 ci.yml 一致改用 RELEASE_PAT
          password: ${{ secrets.RELEASE_PAT }}

      - name: Compute image name
        id: image
        run: |
          REPO=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "image=ghcr.io/${REPO}/opc-mosquitto" >> "$GITHUB_OUTPUT"

      - name: Login to Aliyun ACR
        if: vars.ACR_REGISTRY != ''
        uses: docker/login-action@74a5d142397b4f367a81961eba4e8cd7edddf772 # v3.4.0
        with:
          registry: ${{ vars.ACR_REGISTRY }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@471d1dc4e07e5cdedd4c2171150001c434f0b7a4 # v6.15.0
        with:
          context: ./docker/mosquitto
          push: true
          tags: |
            ${{ steps.image.outputs.image }}:latest
            ${{ steps.image.outputs.image }}:sha-${{ github.sha }}
            ${{ vars.ACR_REGISTRY != '' && format('{0}/{1}/opc-mosquitto:latest', vars.ACR_REGISTRY, vars.ACR_NAMESPACE) || '' }}
            ${{ vars.ACR_REGISTRY != '' && format('{0}/{1}/opc-mosquitto:sha-{2}', vars.ACR_REGISTRY, vars.ACR_NAMESPACE, github.sha) || '' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
